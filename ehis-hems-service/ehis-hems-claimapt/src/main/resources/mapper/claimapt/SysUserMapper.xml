<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paic.ehis.claimapt.mapper.SysUserMapper">

	<select id="selectDept" parameterType="String" resultType="int">
    SELECT dept_id FROM sys_user WHERE user_name=#{username}
	</select>

</mapper> 