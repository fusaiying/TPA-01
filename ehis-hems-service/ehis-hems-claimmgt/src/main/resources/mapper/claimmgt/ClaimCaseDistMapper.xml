<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paic.ehis.claimmgt.mapper.ClaimCaseDistMapper">
    
    <resultMap type="ClaimCaseDist" id="ClaimCaseDistResult">
        <result property="distId"    column="dist_id"    />
        <result property="userId"    column="user_id"    />
        <result property="roleId"    column="role_id"    />
        <result property="isEqually"    column="is_equally"    />
        <result property="rate"    column="rate"    />
        <result property="status"    column="status"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="userName"    column="user_name"    />
        <result property="roleName"    column="role_name"    />
    </resultMap>

    <sql id="selectClaimCaseDistVo">
        select dist_id, user_id, role_id, is_equally, rate, status, create_by, create_time, update_by, update_time from claim_case_dist
    </sql>

    <select id="selectClaimCaseDistList" parameterType="ClaimCaseDist" resultMap="ClaimCaseDistResult">
        <include refid="selectClaimCaseDistVo"/>
        <where>
            <if test="userId != null "> and user_id  like concat('%', #{userId}, '%')</if>
            <if test="roleId != null "> and role_id = #{roleId}</if>
            <if test="isEqually != null  and isEqually != ''"> and is_equally = #{isEqually}</if>
            <if test="rate != null "> and rate = #{rate}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>
    <select id="selectClaimCaseDistListOne" parameterType="ClaimCaseDist" resultMap="ClaimCaseDistResult">
        <include refid="selectClaimCaseDistVo"/>
        <where>
            <if test="userId != null "> and user_id  like concat('%', #{userId}, '%')</if>
            <if test="roleId != null "> and role_id = #{roleId}</if>
            <if test="isEqually != null  and isEqually != ''"> and is_equally = #{isEqually}</if>
            <if test="rate != null "> and rate = #{rate}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>
    <select id="selectClaimCaseDistById" parameterType="Long" resultMap="ClaimCaseDistResult">
        <include refid="selectClaimCaseDistVo"/>
        where dist_id = #{distId}
    </select>
        
    <insert id="insertClaimCaseDist" parameterType="ClaimCaseDist" useGeneratedKeys="true" keyProperty="distId">
        insert into claim_case_dist
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="roleId != null">role_id,</if>
            <if test="isEqually != null and isEqually != ''">is_equally,</if>
            <if test="rate != null">rate,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="roleId != null">#{roleId},</if>
            <if test="isEqually != null and isEqually != ''">#{isEqually},</if>
            <if test="rate != null">#{rate},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateClaimCaseDist" parameterType="ClaimCaseDist">
        update claim_case_dist
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="isEqually != null and isEqually != ''">is_equally = #{isEqually},</if>
            <if test="rate != null">rate = #{rate},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="createBy != null and createBy != ''">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where dist_id = #{distId}
    </update>

    <delete id="deleteClaimCaseDistById" parameterType="Long">
        delete from claim_case_dist where dist_id = #{distId}
    </delete>

    <delete id="deleteClaimCaseDistByIds" parameterType="String">
        delete from claim_case_dist where dist_id in 
        <foreach item="distId" collection="array" open="(" separator="," close=")">
            #{distId}
        </foreach>
    </delete>
    <update id="updateClaimCaseAverage" parameterType="ClaimCaseDist">
        update claim_case_dist
        <trim prefix="SET" suffixOverrides=",">
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="isEqually != null and isEqually != ''">is_equally = #{isEqually},</if>
            <if test="rate != null">rate = #{rate},</if>
        </trim>
        where role_id = #{roleId}
    </update>
    <update id="updateClaimCaseDistOne" parameterType="ClaimCaseDist">
        update claim_case_dist
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="isEqually != null and isEqually != ''">is_equally = #{isEqually},</if>
            <if test="rate != null">rate = #{rate},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="createBy != null and createBy != ''">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where dist_id = #{distId}
    </update>
    <select id="selectClaimCaseDistRate" parameterType="Long" resultMap="ClaimCaseDistResult">
        select rate from claim_case_dist
        where user_id = #{userId}  and role_id=#{roleId}
    </select>
    <select id="selectClaimCaseDist" parameterType="ClaimCaseDist" resultMap="ClaimCaseDistResult">
        select t1.user_id,t1.role_id,t1.rate,t1.status,t2.user_name,t4.role_name , t1.dist_id
        from claim_case_dist t1
        LEFT JOIN sys_user t2  on t1.user_id = t2.user_id
        LEFT JOIN sys_role t4 on t1.role_id = t4.role_id
        <where>
            <if test="userId != null "> and t1.user_id  like concat('%', #{userId}, '%')</if>
            <if test="roleId != null "> and t3.role_id = #{roleId}</if>
            <if test="isEqually != null  and isEqually != ''"> and t1.is_equally = #{isEqually}</if>
            <if test="rate != null "> and t1.rate = #{rate}</if>
            <if test="status != null  and status != ''"> and t1.status = #{status}</if>
        </where>
    </select>
</mapper>